@page "/policy"
@using Intentum.Runtime.Policy
@inject PlaygroundService Service

<PageTitle>IntentPolicy Editor</PageTitle>

<h1>IntentPolicy Editor</h1>

<RuleEditor OnRuleAdded="AddRule" />

<div class="rules-list">
    <h3>Rules (@Service.Policy.Rules.Count)</h3>
    @foreach (var rule in Service.Policy.Rules)
    {
        <div class="rule-item">
            <span class="rule-name">@rule.Name</span>
            <span class="rule-decision">@rule.Decision</span>
        </div>
    }
</div>

@code {
    private void AddRule(PolicyRule rule)
    {
        Service.Policy.AddRule(rule);
    }
}
