# Customer intent example

Intentum example for **customer intent** (purchase, info gathering, support). Uses Mock embedding; no API key.

Run: `dotnet run --project examples/customer-intent`

See [Domain intent templates](../../docs/en/domain-intent-templates.md) for intent names and suggested actor:action.
